"use client";

import { ArrowLeft, ArrowRight, Check, History, Monitor, Moon, Save, Settings2, Sun, UserRound } from "lucide-react";
import { usePathname, useRouter } from "next/navigation";
import { onAuthStateChanged, signOut as firebaseSignOut } from "firebase/auth";
import { useEffect, useState } from "react";
import { AnalysisResults } from "@/components/AnalysisResults";
import { BrandMark } from "@/components/BrandMark";
import { ProcessingSteps } from "@/components/ProcessingSteps";
import { ProductSidebar, MobileMenuButton, type ProfileSummary, type ProductView } from "@/components/ProductSidebar";
import { UploadDropzone } from "@/components/UploadDropzone";
import { AnalysisSchema, type Analysis } from "@/lib/analysis-schema";
import { firebaseAuth } from "@/lib/firebase/client";
import { getAnalysisSession, getUserProfile, listAnalysisSessions, saveAnalysisSession, submitFeedback, updateUserProfile } from "@/lib/firebase/persistence";

type Stage = "reading" | "analyzing" | "saving" | null;
type ThemePreference = "light" | "dark" | "system";
type SaveStatus = "idle" | "saving" | "saved" | "error";
type ProfileData = ProfileSummary & { id: string; createdAt: string; provider: string | null };
type HistorySummary = { id: string; filename: string; file_type: string; extracted_text: string; ocr_confidence: number | null; analysis_result: Analysis; engagement_score: number; created_at: string };
type HistorySession = HistorySummary;

async function readError(response: Response, fallback: string) { try { const payload = (await response.json()) as { error?: string }; return payload.error || fallback; } catch { return fallback; } }
function PageIntro({ eyebrow, title, description }: { eyebrow: string; title: React.ReactNode; description: string }) { return <div className="mb-9"><p className="mb-3 text-sm font-bold uppercase tracking-[0.16em] text-coral">{eyebrow}</p><h1 className="editorial-display max-w-3xl text-4xl font-bold leading-[1.08] tracking-[-0.04em] text-ink sm:text-6xl">{title}</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted">{description}</p></div>; }
function formatDate(value: string) { return new Intl.DateTimeFormat(undefined, { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)); }
function getView(pathname: string): ProductView { if (pathname.startsWith("/dashboard")) return "dashboard"; if (pathname.startsWith("/history")) return "history"; if (pathname.startsWith("/settings")) return "settings"; if (pathname.startsWith("/help")) return "help"; if (pathname.startsWith("/profile")) return "profile"; return "analyze"; }
function ThemeToggle({ preference, onChange }: { preference: ThemePreference; onChange: (theme: ThemePreference) => void }) {
  const nextTheme = preference === "light" ? "dark" : preference === "dark" ? "system" : "light";
  const label = preference === "system" ? "System theme" : preference === "dark" ? "Dark theme" : "Light theme";
  const nextLabel = nextTheme === "system" ? "system theme" : `${nextTheme} theme`;
  const Icon = preference === "dark" ? Moon : preference === "light" ? Sun : Monitor;
  return <button type="button" className="theme-toggle" onClick={() => onChange(nextTheme)} aria-label={`${label}. Switch to ${nextLabel}.`} title={`${label} · switch to ${nextLabel}`}><Icon aria-hidden="true" className="h-4 w-4" /><span>{preference === "system" ? "System" : preference === "dark" ? "Dark" : "Light"}</span></button>;
}

function DashboardView({ onNavigate }: { onNavigate: (view: ProductView) => void }) { return <div className="max-w-5xl"><PageIntro eyebrow="YOUR WORKSPACE" title="A calmer way to sharpen your content." description="SocialForge turns a post, screenshot, or document into a concise editorial brief you can act on." /><div className="grid gap-5 md:grid-cols-2"><section className="surface-card p-7 sm:p-8"><p className="text-sm font-bold uppercase tracking-[0.14em] text-primary">Start here</p><h2 className="mt-4 text-2xl font-bold text-ink">Analyze a piece of content</h2><p className="mt-3 text-base leading-7 text-muted">Upload a PDF or image to extract the source text, review it, and generate recommendations.</p><button type="button" onClick={() => onNavigate("analyze")} className="mt-7 inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3.5 text-base font-bold text-white transition hover:bg-primary-hover focus:outline-none focus-visible:ring-4 focus-visible:ring-primary/25">Open analyzer <ArrowRight aria-hidden="true" className="h-5 w-5" /></button></section><section className="surface-card p-7 sm:p-8"><p className="text-sm font-bold uppercase tracking-[0.14em] text-muted">Your workflow</p><ol className="mt-5 space-y-4 text-base text-ink"><li className="flex items-center gap-3"><span className="step-dot">01</span><span>Extract the source content</span></li><li className="flex items-center gap-3"><span className="step-dot">02</span><span>Review what was read</span></li><li className="flex items-center gap-3"><span className="step-dot">03</span><span>Improve the next draft</span></li></ol></section></div></div>; }

function HistoryView({ onOpen }: { onOpen: (id: string) => void }) {
  const [sessions, setSessions] = useState<HistorySummary[]>([]); const [query, setQuery] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  useEffect(() => { listAnalysisSessions(firebaseAuth.currentUser).then(setSessions).catch((caught: unknown) => setError(caught instanceof Error ? caught.message : "We couldn't load your history.")).finally(() => setLoading(false)); }, []);
  const visible = sessions.filter((session) => `${session.filename} ${session.analysis_result.overallAssessment}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="max-w-5xl"><PageIntro eyebrow="HISTORY" title="Your editorial trail, in one place." description="Revisit completed analyses and keep improving the way you communicate." /><div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><label className="sr-only" htmlFor="history-search">Search history</label><input id="history-search" className="field-input max-w-md" placeholder="Search filename or summary" value={query} onChange={(event) => setQuery(event.target.value)} /><span className="text-sm text-muted">{sessions.length} {sessions.length === 1 ? "analysis" : "analyses"}</span></div>{loading && <div className="surface-card p-8 text-base text-muted">Loading your history…</div>}{error && <div className="surface-warning rounded-xl border px-4 py-3 text-base" role="alert">{error}</div>}{!loading && !error && visible.length === 0 && <section className="surface-card p-8 text-center sm:p-12"><span className="mx-auto flex h-12 w-12 items-center justify-center rounded-xl bg-surface-secondary text-primary"><History aria-hidden="true" className="h-6 w-6" /></span><h2 className="mt-5 text-2xl font-bold text-ink">No analyses yet.</h2><p className="mx-auto mt-3 max-w-md text-base leading-7 text-muted">Your analyzed content will appear here so you can revisit and improve it later.</p></section>}{!loading && visible.length > 0 && <div className="space-y-3">{visible.map((session) => <button key={session.id} type="button" onClick={() => onOpen(session.id)} className="history-item"><span className="flex min-w-0 flex-1 items-start gap-4"><span className="file-type-badge">{session.file_type.split("/").pop()?.toUpperCase() || "FILE"}</span><span className="min-w-0 text-left"><span className="block truncate text-lg font-bold text-ink">{session.filename}</span><span className="mt-1 block text-sm leading-6 text-muted">{session.analysis_result.overallAssessment}</span><span className="mt-2 block text-sm text-muted">{formatDate(session.created_at)}</span></span></span><span className="history-score">{session.engagement_score}<small>/100</small></span></button>)}</div>}</div>;
}

function HistoryDetailView({ id, onBack }: { id: string; onBack: () => void }) {
  const [session, setSession] = useState<HistorySession | null>(null); const [error, setError] = useState("");
  useEffect(() => { getAnalysisSession(firebaseAuth.currentUser, id).then(setSession).catch((caught: unknown) => setError(caught instanceof Error ? caught.message : "We couldn't load that analysis.")); }, [id]);
  if (error) return <div className="max-w-3xl"><button type="button" onClick={onBack} className="back-link"><ArrowLeft aria-hidden="true" className="h-4 w-4" />Back to history</button><div className="surface-warning mt-8 rounded-xl border px-4 py-3 text-base" role="alert">{error}</div></div>;
  if (!session) return <div className="surface-card p-8 text-base text-muted">Loading saved analysis…</div>;
  const parsed = AnalysisSchema.safeParse(session.analysis_result); if (!parsed.success) return <div className="surface-warning rounded-xl border px-4 py-3 text-base">This saved analysis has an invalid result format.</div>;
  return <div className="max-w-5xl"><button type="button" onClick={onBack} className="back-link"><ArrowLeft aria-hidden="true" className="h-4 w-4" />Back to history</button><div className="mb-7 mt-6"><p className="text-sm font-bold uppercase tracking-[0.14em] text-coral">SAVED ANALYSIS</p><h1 className="editorial-display mt-3 text-4xl font-bold tracking-[-0.04em] text-ink">{session.filename}</h1><p className="mt-3 text-base text-muted">{formatDate(session.created_at)} · {session.file_type} · {session.ocr_confidence !== null ? `${session.ocr_confidence}% OCR confidence` : "Extracted text"}</p></div><section className="mb-9 overflow-hidden rounded-2xl border border-line bg-surface"><div className="border-b border-line px-5 py-4 sm:px-7"><h2 className="text-lg font-bold text-ink">Extracted text</h2></div><pre className="max-h-72 overflow-auto whitespace-pre-wrap px-5 py-5 text-base leading-8 text-ink/85 sm:px-7">{session.extracted_text}</pre></section><div className="mb-5"><p className="text-sm font-bold uppercase tracking-[0.14em] text-coral">YOUR EDITORIAL BRIEF</p><h2 className="mt-2 text-3xl font-bold text-ink">What your content is saying</h2></div><AnalysisResults analysis={parsed.data} /></div>;
}

function HelpView() {
  const steps = ["Upload a PDF or image", "Extract the text", "Review the extracted content", "Analyze the content", "Improve the post"];
  const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const [status, setStatus] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  const submitFeedbackForm = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); setStatus(""); setError(""); try { await submitFeedback(firebaseAuth.currentUser, email, message); setStatus("Thanks — your feedback was sent."); setEmail(""); setMessage(""); } catch (caught) { setError(caught instanceof Error ? caught.message : "We couldn't send your feedback."); } finally { setBusy(false); } };
  return <div className="max-w-5xl"><PageIntro eyebrow="HELP" title="A quick guide to SocialForge." description="A focused workflow for turning raw content into a clearer, more useful post." /><div className="grid gap-5 lg:grid-cols-[1.05fr_.95fr]"><section className="surface-card p-7 sm:p-8"><h2 className="text-xl font-bold text-ink">How it works</h2><ol className="mt-6 space-y-5">{steps.map((step, index) => <li key={step} className="flex items-center gap-4 text-base text-ink"><span className="step-dot">{String(index + 1).padStart(2, "0")}</span><span>{step}</span></li>)}</ol><div className="mt-8 border-t border-line pt-6"><h2 className="text-xl font-bold text-ink">Supported files</h2><p className="mt-3 text-base leading-7 text-muted">PDF, PNG, JPG, and JPEG files up to 10 MB.</p></div></section><section className="surface-card p-7 sm:p-8"><p className="text-sm font-bold uppercase tracking-[0.14em] text-primary">FEEDBACK</p><h2 className="mt-3 text-2xl font-bold text-ink">Have feedback?</h2><p className="mt-3 text-base leading-7 text-muted">Tell us what worked, what didn&apos;t, or what you&apos;d like to see next.</p><form onSubmit={submitFeedbackForm} className="mt-6 space-y-4"><label className="field-label">Your email <span className="font-normal text-muted">(optional)</span><input className="field-input" type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /></label><label className="field-label">Your message<textarea className="field-input min-h-32 resize-y" value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Tell us what you think…" required minLength={3} maxLength={2000} /></label><button type="submit" disabled={busy} className="inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3.5 text-base font-bold text-white transition hover:bg-primary-hover focus:outline-none focus-visible:ring-4 focus-visible:ring-primary/25 disabled:opacity-60">{busy ? "Sending…" : "Send feedback"}<ArrowRight aria-hidden="true" className="h-5 w-5" /></button>{(status || error) && <p className={`rounded-xl border px-4 py-3 text-sm ${error ? "surface-warning" : "border-primary/25 bg-surface-secondary text-ink"}`} role="status">{status || error}</p>}</form><p className="mt-6 text-sm text-muted">Need direct help? <a className="font-bold text-primary hover:underline" href="mailto:support@socialforge.app">Email support</a></p></section></div></div>;
}

function SettingsView({ themePreference, onThemeChange }: { themePreference: ThemePreference; onThemeChange: (theme: ThemePreference) => void }) { const options = [{ id: "light", label: "Light", description: "A bright editorial canvas.", icon: Sun }, { id: "dark", label: "Dark", description: "Lower contrast for focused review.", icon: Moon }, { id: "system", label: "System", description: "Follow your device preference.", icon: Monitor }] as const; return <div className="max-w-4xl"><PageIntro eyebrow="SETTINGS" title="Make the workspace feel like yours." description="Choose how SocialForge should appear on this device. Your preference is saved locally." /><section className="surface-card p-6 sm:p-8"><div className="flex items-start gap-4"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-surface-secondary text-primary"><Settings2 aria-hidden="true" className="h-5 w-5" /></span><div><h2 className="text-xl font-bold text-ink">Appearance</h2><p className="mt-2 text-base text-muted">Select a theme for the analyzer and results.</p></div></div><div className="mt-7 grid gap-4 sm:grid-cols-3">{options.map(({ id, label, description, icon: Icon }) => <button key={id} type="button" aria-pressed={themePreference === id} onClick={() => onThemeChange(id)} className={`theme-option ${themePreference === id ? "theme-option-active" : ""}`}><span className="flex items-center justify-between"><Icon aria-hidden="true" className="h-5 w-5" /><span className="theme-check">{themePreference === id ? <Check aria-hidden="true" className="h-4 w-4" /> : null}</span></span><span className="mt-6 block text-left text-base font-bold">{label}</span><span className="mt-2 block text-left text-sm leading-6 opacity-75">{description}</span></button>)}</div></section></div>; }

function ProfileView({ profile, onProfileChange }: { profile: ProfileData | null; onProfileChange: (profile: ProfileData) => void }) {
  const router = useRouter(); const [displayName, setDisplayName] = useState(profile?.displayName || ""); const [avatarUrl, setAvatarUrl] = useState(profile?.avatarUrl || ""); const [editing, setEditing] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  const save = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); setError(""); setMessage(""); try { const profile = await updateUserProfile(firebaseAuth.currentUser, displayName, avatarUrl || null); onProfileChange(profile); setEditing(false); setMessage("Profile updated."); } catch (caught) { setError(caught instanceof Error ? caught.message : "We couldn't update your profile."); } finally { setBusy(false); } };
  const signOut = async () => { await firebaseSignOut(firebaseAuth); router.replace("/login"); };
  if (!profile) return <div className="surface-card p-8 text-base text-muted">Loading your profile…</div>;
  return <div className="max-w-3xl"><PageIntro eyebrow="YOUR PROFILE" title="Your account, at a glance." description="Manage the identity attached to your saved analyses." /><section className="surface-card p-7 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-5"><div className="flex items-center gap-4"><span className="profile-avatar profile-avatar-large">{profile.avatarUrl ? <span className="profile-avatar-image" style={{ backgroundImage: "url(" + profile.avatarUrl + ")" }} role="img" aria-label="Profile picture" /> : <UserRound aria-hidden="true" className="h-7 w-7" />}</span><div><h2 className="text-2xl font-bold text-ink">{profile.displayName || "Your Profile"}</h2><p className="mt-1 text-base text-muted">{profile.email}</p></div></div><button type="button" onClick={() => setEditing((current) => !current)} className="secondary-button">{editing ? "Cancel" : "Edit profile"}</button></div>{editing ? <form onSubmit={save} className="mt-8 space-y-4 border-t border-line pt-7"><label className="field-label">Name<input className="field-input" value={displayName} onChange={(event) => setDisplayName(event.target.value)} required maxLength={80} /></label><label className="field-label">Avatar URL <span className="font-normal text-muted">(optional)</span><input className="field-input" type="url" value={avatarUrl} onChange={(event) => setAvatarUrl(event.target.value)} placeholder="https://…" /></label><button type="submit" disabled={busy} className="rounded-xl bg-primary px-5 py-3.5 text-base font-bold text-white disabled:opacity-60">{busy ? "Saving…" : "Save profile"}</button>{(message || error) && <p className={error ? "surface-warning rounded-xl border px-4 py-3 text-sm" : "rounded-xl border border-primary/25 bg-surface-secondary px-4 py-3 text-sm text-ink"} role="status">{message || error}</p>}</form> : <dl className="mt-8 grid gap-5 border-t border-line pt-7 sm:grid-cols-2"><div><dt className="text-sm font-bold uppercase tracking-[0.12em] text-muted">Connected provider</dt><dd className="mt-2 text-base font-semibold text-ink">{profile.provider || "Email and password"}</dd></div><div><dt className="text-sm font-bold uppercase tracking-[0.12em] text-muted">Account created</dt><dd className="mt-2 text-base font-semibold text-ink">{formatDate(profile.createdAt)}</dd></div></dl>}<button type="button" onClick={signOut} className="mt-8 inline-flex items-center gap-2 text-base font-bold text-coral hover:underline"><UserRound aria-hidden="true" className="h-5 w-5" />Sign out</button></section></div>;
}

export function AnalyzerWorkspace() {
  const pathname = usePathname() || "/analyze"; const router = useRouter(); const view = getView(pathname); const [authReady, setAuthReady] = useState(false); const [mobileOpen, setMobileOpen] = useState(false); const [themePreference, setThemePreference] = useState<ThemePreference>("system"); const [profile, setProfile] = useState<ProfileData | null>(null); const [file, setFile] = useState<File | null>(null); const [selectionError, setSelectionError] = useState(""); const [stage, setStage] = useState<Stage>(null); const [extractedText, setExtractedText] = useState(""); const [sourceType, setSourceType] = useState(""); const [ocrConfidence, setOcrConfidence] = useState<number | null>(null); const [ocrWarning, setOcrWarning] = useState(""); const [analysis, setAnalysis] = useState<Analysis | null>(null); const [error, setError] = useState(""); const [saveStatus, setSaveStatus] = useState<SaveStatus>("idle"); const [saveMessage, setSaveMessage] = useState("");
  useEffect(() => onAuthStateChanged(firebaseAuth, (user) => { if (!user) router.replace("/login"); else setAuthReady(true); }), [router]);
  useEffect(() => { const stored = window.localStorage.getItem("socialforge-theme"); if (stored === "light" || stored === "dark" || stored === "system") { const timeout = window.setTimeout(() => setThemePreference(stored), 0); return () => window.clearTimeout(timeout); } }, []);
  useEffect(() => { const media = window.matchMedia("(prefers-color-scheme: dark)"); const applyTheme = () => { document.documentElement.dataset.theme = themePreference === "system" ? (media.matches ? "dark" : "light") : themePreference; }; applyTheme(); window.localStorage.setItem("socialforge-theme", themePreference); if (themePreference === "system") media.addEventListener?.("change", applyTheme); return () => media.removeEventListener?.("change", applyTheme); }, [themePreference]);
  useEffect(() => { if (!authReady) return; getUserProfile(firebaseAuth.currentUser).then(setProfile).catch(() => undefined); }, [authReady]);
  const navigate = (nextView: ProductView) => router.push(`/${nextView}`);
  const onFileChange = (nextFile: File | null, nextError?: string) => { setFile(nextFile); setSelectionError(nextError || ""); setError(""); setExtractedText(""); setAnalysis(null); setSourceType(""); setOcrConfidence(null); setOcrWarning(""); setSaveStatus("idle"); setSaveMessage(""); };
  const analyze = async () => { if (!file || stage) return; setError(""); setExtractedText(""); setAnalysis(null); setOcrConfidence(null); setOcrWarning(""); setSaveStatus("idle"); setSaveMessage(""); setStage("reading"); try { const formData = new FormData(); formData.append("file", file); const extractionResponse = await fetch("/api/extract", { method: "POST", body: formData }); if (!extractionResponse.ok) throw new Error(await readError(extractionResponse, "We couldn't extract text from that file.")); const extraction = await extractionResponse.json() as { text: string; sourceType: string; ocrConfidence?: number; ocrWarning?: string }; setExtractedText(extraction.text); setSourceType(extraction.sourceType); setOcrConfidence(typeof extraction.ocrConfidence === "number" ? extraction.ocrConfidence : null); setOcrWarning(extraction.ocrWarning || ""); setStage("analyzing"); const analysisResponse = await fetch("/api/analyze", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text: extraction.text }) }); if (!analysisResponse.ok) throw new Error(await readError(analysisResponse, "We couldn't analyze that content.")); const result = await analysisResponse.json() as { analysis: Analysis }; setAnalysis(result.analysis); setStage("saving"); setSaveStatus("saving"); try { await saveAnalysisSession(firebaseAuth.currentUser, { filename: file.name, fileType: file.type || "unknown", extractedText: extraction.text, ocrConfidence: typeof extraction.ocrConfidence === "number" ? extraction.ocrConfidence : null, analysis: result.analysis }); setSaveStatus("saved"); setSaveMessage("Analysis saved to your history."); } catch (caught) { setSaveStatus("error"); setSaveMessage(caught instanceof Error ? caught.message : "Your analysis is ready, but it couldn't be saved to history."); } } catch (caught) { setError(caught instanceof Error ? caught.message : "Something went wrong. Please try again."); } finally { setStage(null); } };
  const renderAnalyzer = () => <><div className="grid gap-8 xl:grid-cols-[.86fr_1.14fr] xl:gap-12"><div><PageIntro eyebrow="MAKE EVERY WORD COUNT" title={<>Turn raw posts into <span className="text-primary">stronger signals.</span></>} description="Upload a post, screenshot, or document. Get an editor's eye on the hook, message, and next best move." /><div className="mt-8 hidden items-center gap-5 text-sm font-semibold text-muted sm:flex"><span>01 · Extract</span><span className="h-px w-7 bg-line" /><span>02 · Understand</span><span className="h-px w-7 bg-line" /><span>03 · Improve</span></div></div><section className="surface-card p-5 shadow-card sm:p-7" aria-labelledby="upload-heading"><div className="mb-6 flex items-start justify-between gap-4"><div><h2 id="upload-heading" className="text-xl font-bold text-ink">Bring your content</h2><p className="mt-2 text-base text-muted">We&apos;ll read it, then help you sharpen it.</p></div><span className="rounded-full bg-surface-secondary px-3 py-1.5 text-sm font-bold text-primary">Private by default</span></div><UploadDropzone file={file} onFileChange={onFileChange} />{selectionError && <p className="surface-warning mt-3 rounded-lg border px-3 py-2 text-sm font-medium" role="alert">{selectionError}</p>}<button type="button" onClick={analyze} disabled={!file || Boolean(stage)} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-5 py-4 text-base font-bold text-white shadow-lg shadow-primary/15 transition hover:bg-primary-hover focus:outline-none focus-visible:ring-4 focus-visible:ring-primary/25 disabled:cursor-not-allowed disabled:bg-primary-muted disabled:shadow-none">{stage ? <><span className="h-4 w-4 animate-spin rounded-full border-2 border-white/35 border-t-white" />{stage === "reading" ? "Extracting content" : stage === "analyzing" ? "Analyzing content" : "Saving analysis"}</> : <>Analyze my content <ArrowRight aria-hidden="true" className="h-5 w-5" /></>}</button></section></div><div className="mt-10"><ProcessingSteps stage={stage} />{error && <div className="surface-warning mb-6 flex items-start gap-3 rounded-xl border px-4 py-3 text-base" role="alert"><span aria-hidden="true" className="font-bold">!</span><p>{error}</p></div>}{saveMessage && <p className={`mb-6 flex items-center gap-2 rounded-xl border px-4 py-3 text-base ${saveStatus === "error" ? "surface-warning" : "border-primary/25 bg-surface-secondary text-ink"}`} role="status">{saveStatus === "saved" ? <Save aria-hidden="true" className="h-5 w-5 text-primary" /> : null}{saveMessage}</p>}{extractedText && <section className="mb-10 overflow-hidden rounded-2xl border border-line bg-surface shadow-sm" aria-labelledby="extracted-heading"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-line px-5 py-5 sm:px-7"><div><h2 id="extracted-heading" className="text-xl font-bold text-ink">Extracted text</h2><p className="mt-2 text-sm text-muted">{sourceType}{ocrConfidence !== null ? ` · ${ocrConfidence}% OCR confidence` : ""} · Review the source before recommendations</p></div><span className="rounded-full bg-canvas px-3 py-1.5 text-sm font-semibold text-muted">{extractedText.length.toLocaleString()} characters</span></div>{ocrWarning && <p className="surface-warning border-b px-5 py-3 text-base font-medium sm:px-7" role="status">{ocrWarning}</p>}<pre className="max-h-80 overflow-auto whitespace-pre-wrap px-5 py-6 text-base leading-8 text-ink/85 sm:px-7">{extractedText}</pre></section>}{analysis && <section aria-labelledby="analysis-heading"><div className="mb-6 flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm font-bold text-coral">YOUR EDITORIAL BRIEF</p><h2 id="analysis-heading" className="mt-2 text-3xl font-bold tracking-tight text-ink sm:text-4xl">What your content is saying</h2></div><p className="text-base text-muted">Grounded in the text you provided.</p></div><AnalysisResults analysis={analysis} /></section>}</div></>;
  const renderView = () => { if (view === "dashboard") return <DashboardView onNavigate={navigate} />; if (view === "history") { const parts = pathname.split("/").filter(Boolean); return parts.length > 1 ? <HistoryDetailView id={parts[1]} onBack={() => router.push("/history")} /> : <HistoryView onOpen={(id) => router.push(`/history/${id}`)} />; } if (view === "settings") return <SettingsView themePreference={themePreference} onThemeChange={setThemePreference} />; if (view === "help") return <HelpView />; if (view === "profile") return <ProfileView key={profile?.id || "profile-loading"} profile={profile} onProfileChange={setProfile} />; return renderAnalyzer(); };
  if (!authReady) return <div className="grid min-h-screen place-items-center bg-canvas"><p className="text-base text-muted">Checking your sign-in…</p></div>;
  return <div className="grid-texture min-h-screen lg:flex"><ProductSidebar activeView={view} mobileOpen={mobileOpen} profile={profile} onNavigate={navigate} onMobileToggle={setMobileOpen} /><div className="min-w-0 flex-1"><header className="flex items-center justify-between border-b border-line bg-surface/90 px-4 py-4 backdrop-blur lg:hidden"><div className="flex items-center gap-3"><BrandMark compact /><div><p className="text-[14px] font-extrabold tracking-[0.15em] text-ink">SOCIALFORGE</p><p className="text-[10px] font-bold uppercase tracking-[0.15em] text-muted">Content intelligence</p></div></div><div className="flex items-center gap-2"><ThemeToggle preference={themePreference} onChange={setThemePreference} /><MobileMenuButton onClick={() => setMobileOpen(true)} /></div></header><main className="mx-auto min-h-[calc(100vh-73px)] max-w-7xl px-4 pb-24 pt-9 sm:px-7 sm:pt-14 lg:min-h-screen lg:px-12 xl:px-16"><div className="mb-6 hidden justify-end lg:mb-3 lg:flex"><ThemeToggle preference={themePreference} onChange={setThemePreference} /></div>{renderView()}</main><footer className="border-t border-line bg-surface"><div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-7 text-sm text-muted sm:flex-row sm:items-center sm:justify-between sm:px-7 lg:px-12 xl:px-16"><span>SocialForge · A focused content review workspace</span><span>Your analyses are saved securely to your account.</span></div></footer></div></div>;
}




